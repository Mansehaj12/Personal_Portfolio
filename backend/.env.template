# Server Port
PORT=5000

# MongoDB Connection String (leave blank or invalid to auto-trigger JSON fallback database)
MONGO_URI=mongodb://localhost:27017/portfolio

# Nodemailer configuration for Contact Form forwarding
# For Gmail, you need to use an "App Password" (https://myaccount.google.com/apppasswords)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password

# Destination email where contact notifications should be sent
RECEIVER_EMAIL=sehajpreetsingh480@gmail.com
